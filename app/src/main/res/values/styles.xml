<resources>

	<!-- Standard application theme. -->
	<style name="StandardTheme" parent="Theme.AppCompat.Light.NoActionBar">
	</style>

	<!-- Setting Activity theme -->
	<style name="SettingActivityTheme" parent="StandardTheme">
		<item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
	</style>

	<!-- Status bar is translucent -->
	<style name="TranslucentStatusBarTheme" parent="StandardTheme" />

	<!-- Translucent Activity theme -->
	<style name="TranslucentActivityTheme" parent="TranslucentStatusBarTheme">
		<item name="android:windowBackground">@android:color/transparent</item>
		<item name="android:colorBackgroundCacheHint">@null</item>
		<item name="android:windowIsTranslucent">true</item>
	</style>

	<!-- Page Indicator -->
	<style name="PageIndicator">
		<item name="android:layout_width">wrap_content</item>
		<item name="android:layout_height">wrap_content</item>
		<item name="android:layout_centerVertical">true</item>
		<item name="android:scaleType">centerInside</item>
		<item name="android:alpha">0.6</item>
		<item name="android:adjustViewBounds">true</item>
	</style>

	<!-- Save Button -->
	<style name="SaveButton">
		<item name="android:layout_width">match_parent</item>
		<item name="android:layout_height">80dp</item>
		<item name="android:layout_marginLeft">20dp</item>
		<item name="android:layout_marginRight">20dp</item>
		<item name="android:layout_centerHorizontal">true</item>
	</style>

	<style name="SaveButtonDetailText">
		<item name="android:gravity">center</item>
		<item name="android:textSize">14sp</item>
		<item name="android:textColor">@color/black</item>
		<item name="android:singleLine">true</item>
	</style>

</resources>
