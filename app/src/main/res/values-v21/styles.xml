<resources>

	<!-- Standard application theme. -->
	<style name="StandardTheme" parent="Theme.AppCompat.Light.NoActionBar">
		<item name="android:colorPrimary">@color/bloody</item>
		<item name="android:colorPrimaryDark">@color/bloody</item>
	</style>

	<!-- Status bar is translucent -->
	<style name="TranslucentStatusBarTheme" parent="StandardTheme">
		<item name="android:statusBarColor">@android:color/transparent</item>
	</style>

</resources>
